# AI Provider (openai, anthropic, mock)
# Note: This is only used for initial default. AI provider and keys are now
# managed through the Settings page in the app and stored in the database.
VITE_AI_PROVIDER=mock
